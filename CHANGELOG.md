# Changelog

## [1.15.0] - 2026-08-27

### Added
- **대화 보기(메신저 뷰)를 헤더 설정으로 승격**: `끄기 / 말풍선(카톡) / 디스코드` 3가지 표시 방식. 캐릭터 테마·강도와 무관하게 아무 테마에서나 켤 수 있다.
- **디스코드 스타일**: 아바타 + 이름 + 시각이 왼쪽 정렬로 붙고, 같은 사람이 5분 안에 연속으로 말하면 헤더/아바타를 생략해 묶는다. 도구 실행은 왼쪽 강조선이 붙은 모노스페이스 줄로, 도구 결과는 접힌 카드로 표시.
- **버전 · 업데이트 UI**: 헤더에 버전 팝오버 추가 — 현재 버전, 업데이트 확인 버튼, 다운로드 진행률, "재시작해서 설치" 버튼. 그동안 자동 업데이트는 앱 시작 시 조용히 돌기만 하고 사용자가 직접 확인할 방법이 없었다.
  - Electron: `update-check` / `update-install` IPC 와 `cth-update-status` 이벤트 추가(확인 중·새 버전·다운로드 %·설치 준비·오류).
  - 브라우저/탭 모드에서는 자동 업데이트가 없다는 안내를 표시.
- 대화 로그 시각 표시를 위해 `/api/transcript` 항목에 `timestamp` 추가.

### Fixed
- **컴포즈로 메시지를 보내도 메신저 영역에 아무것도 안 보이던 문제**: 대화 로그가 `캐릭터 테마 + 강도 Lv3` 일 때만 표시되도록 묶여 있어서, 기본 테마에서 컴포즈를 켜고 메시지를 보내면 터미널로만 가고 대화창에는 아무것도 안 나왔다.
  - 대화 로그를 테마와 분리해 독립 설정으로 변경.
  - **로컬 에코 추가** — 보낸 메시지는 세션 기록(jsonl)에 반영되기 전에도 즉시 대화창에 표시되고, 기록이 따라잡으면 자동으로 합쳐진다. 셸 세션처럼 기록이 아예 없는 경우에도 내가 보낸 메시지는 보인다.
  - 대화 보기를 켜면 컴포즈 입력창이 꺼져 있어도 같이 켜준다(안 그러면 아무것도 안 보임).

## [1.14.2] - 2026-08-27

### Fixed
- **릴리즈에 Windows exe 와 `latest.yml` 이 누락되던 문제**(1.14.1 에서 발생): Windows/macOS 워크플로가 각자 마지막에 draft 를 해제하는 구조라, 먼저 끝난 mac 이 릴리즈를 발행해버리면 뒤늦게 끝난 Windows 의 `electron-builder` 가 "이미 published" 를 보고 업로드를 조용히 건너뛰었다(스텝은 success). Windows 사용자는 auto-update 를 받을 수 없었음.
  - 태그 push 를 처리하는 워크플로를 **`release.yml` 하나로 통합**하고, `publish` job 이 `needs: [windows, mac]` 로 **두 빌드가 모두 끝난 뒤에만** draft 를 해제하도록 변경.
  - 각 빌드 job 에 **업로드 검증 스텝** 추가 — 릴리즈에 exe/dmg 와 `latest*.yml` 이 실제로 있는지 확인하고 없으면 빌드를 실패시킨다.
  - `build-windows.yml` / `build-mac.yml` 은 수동 테스트 빌드 전용으로 축소(태그 트리거·발행 제거).

## [1.14.1] - 2026-08-27

### Fixed
- **릴리즈가 draft 로 멈춰 배포가 안 되던 문제**: `electron-builder` 가 설치파일을 올린 뒤에도 릴리즈가 draft 로 남아 사용자에게 보이지 않고 auto-update 도 잡지 못했음. CI 워크플로(win/mac)에 `Publish release (draft 해제)` 스텝을 추가해 **태그 push 만으로 정식 발행까지 완료**되도록 수정.

### Changed
- `CLAUDE.md` 에 배포(릴리즈 발행) 절차 문서화 — 발행 확인 방법과 실패 시 폴백 포함.

## [1.14.0] - 2026-08-27

### Added
- **캐릭터 테마**: 치이카와(하치와레·우사기·모몽가·랏코) + 동물의숲(뽀야미·글레이·쭈니) 7종. 색을 손으로 고르지 않고 **캐릭터 이미지에서 대표색을 추출해 팔레트 전체(배경·패널·테두리·강조·터미널색)를 생성**한다. 뽑힌 색 중 어떤 것을 기준색으로 쓸지 테마 팝오버에서 눌러 바꿀 수 있음.
  - **모드**: 다크 / 파스텔(밝은 UI + 잉크색 터미널로 눈부심 방지).
  - **강도 3단계**: Lv1 색만 · Lv2 색+이미지(헤더·패널바·뷰어 아바타, 캐릭터 커서, 둥근 모서리) · Lv3 풀캐릭터(메신저형 입력창 + 터미널 워터마크 + 배경 타일 + 캐릭터 말투 플레이스홀더).
- **캐릭터 이미지 폴더**: `~/.claude-terminal-hub/theme-assets/` 를 읽어 아바타·커서·워터마크에 사용. 테마 팝오버의 드롭존에 이미지를 끌어다 놓거나 클릭해서 넣을 수 있고, 넣는 즉시 팔레트가 그 이미지 색으로 다시 만들어진다. **이미지는 저장소·설치파일에 포함되지 않는다**(각자 PC 에만 존재).
  - 서버: `GET /api/theme-assets`(목록), `POST /api/theme-assets`(저장), `/theme-assets/*`(정적 제공).
- **커서 직접 바꾸기**: 헤더에 커서 설정 추가 — 이미지(테마 캐릭터 / 내 이미지 / 기본 화살표), 크기(16~64px), 원본 그대로/얼굴 크롭, 핫스팟, 화살표 촉 겹치기, 적용 범위. 원본 이미지를 canvas 로 축소해 만들며 캐릭터가 아닌 아무 이미지나 쓸 수 있다.
- **메신저형 입력창(Lv3)**: 컴포즈 위에 대화가 말풍선으로 쌓인다(세션 뷰어와 같은 `/api/transcript` 재사용). `대화만` 버튼으로 터미널을 접고 대화만 보는 모드로 전환.
- **글꼴 번들**: JetBrains Mono · Fira Code · Pretendard · 고운돋움 · 주아를 앱에 동봉(전부 OFL 1.1). 오프라인에서도 동작.
- **화면(UI) 글꼴 선택**: 터미널 글꼴과 별개로 헤더·목록·입력창 등 UI 전체 글꼴을 고를 수 있음. 기본값 Pretendard.

### Fixed
- **글꼴을 바꿔도 거의 안 바뀌던 문제**(3중 원인).
  - UI 전체가 CSS 에 `"Segoe UI"` 로 하드코딩돼 있어 글꼴 설정과 무관하게 고정돼 있었음 → `--ui-font` 변수로 교체.
  - 목록의 JetBrains Mono / Fira Code 가 번들되지 않아 PC 에 없으면 조용히 Consolas 로 폴백됐음 → 번들 + 목록에 `(미설치)` 표시.
  - 캔버스 렌더러 글리프 캐시 때문에 교체 후에도 이전 글자가 남을 수 있었음 → `clearTextureAtlas()` + `refresh()` 호출.

## [1.13.1] - 2026-08-13

### Added
- **세션에서 변경된 파일 전체 목록**: 지금까지는 Claude 가 만들거나 고친 파일 중 마지막 하나만 볼 수 있었는데, 헤더의 아티팩트 칩을 클릭하면 세션 전체에서 손댄 파일 목록이 드롭다운으로 뜨고 각 항목을 바로 미리보기할 수 있음.

### Fixed
- `electron-builder` publish 가 기본값(draft)으로 릴리즈를 만들어 실제 사용자에게 안 보이고 auto-update 도 안 잡던 문제 — `releaseType: "release"` 로 바로 정식 발행되게 수정.

## [1.13.0] - 2026-08-13

### Added
- **자동 업데이트**: `electron-updater` 연동. 앱 실행 시 GitHub Release 를 백그라운드로 확인·다운로드하고, 받아지면 지금 재시작할지 물어봄(아니면 다음 종료 시 자동 설치). 태그 push 시 `electron-builder` 가 직접 Release 를 만들어 설치파일+`latest.yml` 을 올림.
- **레이아웃 자동 저장/복원**: 마지막에 열어둔 패널(터미널·미리보기·뷰어) 배치와 분할 크기, 최소화(트레이) 상태까지 프로필별로 기억했다가 재시작 시 그대로 복원.
- **전체 세션 목록**: LNB에 "모든 세션" 토글 추가 — 닫힌 세션까지 개수 제한 없이(서버 상한 500) 나열, 각 세션의 최초/마지막 입력 시각을 jsonl 에서 파싱해 표시.

### Changed
- 패널 기본 배치 개선: 세션을 열 때마다 옆으로 계속 새 컬럼이 생기던 문제 수정 — 컬럼당 2행까지 채운 뒤에만 새 컬럼 생성.
- 패널 사이 여백/거터 축소, 헤더의 '뷰어'·'자동' 버튼 제거.
- 저장소를 public 으로 전환, README 추가.

### Fixed
- 패키징된 exe 에서 서버(`server.js`) 자식 프로세스가 asar 내부 경로 때문에 기동 실패하던 문제 — `asar` 패키징 비활성화로 해결.
- VC++ 재배포 패키지가 없는 사용자 PC에서 `node-pty` 로드 실패하던 문제 — 설치 파일에 재배포 패키지를 번들해 설치 시 자동 설치.
- 서버 기동 실패 시 원인 파악이 안 되던 문제 — 로그를 남기고 에러 화면에 바로 표시.

## [1.12.0] - 2026-08-12

### Added
- **Windows exe 배포**: `electron-builder` 도입 — `npm run dist:win` 으로 NSIS 설치 파일(`.exe`)을 빌드해 일반 사용자에게 git/Node 설치 없이 배포 가능. `node-pty` 네이티브 모듈은 빌드 시 Electron ABI 로 자동 리빌드(`asarUnpack` 처리로 patch 후에도 정상 로드).
  - GitHub Actions `build-windows.yml`: `v*.*.*` 태그 push 또는 수동 실행 시 `windows-latest` 러너에서 exe 빌드 → 워크플로 아티팩트 업로드 + 태그 push 인 경우 GitHub Release 에 첨부. (네이티브 모듈 크로스컴파일은 Linux 에서 불가능해 Windows 러너 필요.)

## [1.11.0] - 2026-08-04

### Added
- **세션 뷰어 (대화·서브에이전트 카드 뷰)**: 터미널 패널 바의 “뷰어” 버튼을 누르면 오른쪽에 그 세션의 대화를 카드로 보여주는 패널이 열린다. 세션 jsonl 트랜스크립트를 파싱해 **사용자/AI 메시지, 도구 실행, 서브에이전트(Task) 작업, 도구 결과**를 구분해 표시하고 2초마다 자동 갱신(하단이면 자동 스크롤). 터미널로만 보던 서브에이전트 작업을 한눈에.
  - 서버: `GET /api/transcript?key=<패널키>` — 살아있는 세션의 jsonl 을 파싱해 최근 500개 메시지 반환(도구 미리보기: Task=서브에이전트/설명, Edit=파일경로, Bash=명령 등).
  - 뷰어는 DOM 전용 패널(스플릿/도킹 됨) — 같은 세션에 중복으로 열리지 않음, 닫으면 폴링 정지.

## [1.10.0] - 2026-08-04

### Added
- **컴포즈 입력창 (선택 토글)**: 헤더 키보드 아이콘으로 켜면 각 터미널 패널 아래에 GUI 식 입력창이 나타난다. **Enter 전송 · Shift+Enter 줄바꿈**, 빠른 버튼(중단 Esc · 모드 Shift+Tab · /clear · 이미지 경로 추가 · 보내기). 입력값은 그대로 pty 로 전달되어 claude 에 입력된다.
  - **터미널·단축키·붙여넣기는 그대로 사용 가능** — 컴포즈는 추가 입력 수단일 뿐, 터미널을 대체하지 않는다. **토글 꺼짐이 기본**이라 켜지 않으면 기존과 100% 동일.
  - 이미지 버튼: Electron 에서 파일 선택 → 경로를 입력창에 삽입(claude 가 이미지 경로 인식). 상태는 `localStorage` 저장.

## [1.9.0] - 2026-08-04

### Added
- **세션 최소화 + 트레이**: 패널 바에 최소화(—) 버튼 추가. 최소화하면 레이아웃에서 숨기되 **term/ws 를 그대로 유지 → claude 는 백그라운드에서 계속 실행**. 상단 트레이에 칩으로 쌓이고(초록=실행중/강조색=완료), 칩 클릭 시 복원, 칩의 ✕ 로 최소화 상태에서 바로 종료. 최소화된 세션이 없으면 트레이는 숨김.
- **도움말 오버레이**: 헤더에 `?` 버튼 추가 → 세션/패널 조작, 터미널 단축키(Ctrl+U/W/K/C/D/A/E, 붙여넣기, 글자크기), Claude 명령어(`/rename` `/clear` `/compact`, Shift+Tab)를 한눈에. Esc 또는 바깥 클릭으로 닫힘. 기존 편의(색상 테마 · /rename · 스플릿/도킹 등)는 그대로.

### Changed
- **커서를 테마 강조색 게임풍 화살표로 변경**: 곰발바닥 커서 제거. 각진 화살표 + 흰 테두리 + 옅은 그림자로 바꾸고, **현재 테마 강조색을 따라간다**(강조색 변경 시 커서 색도 즉시 갱신). 클릭 가능한 요소 위에서는 밝게 빛나는(하이라이트) 화살표. `app.js` 의 `applyCursors()` 가 강조색을 읽어 data-URI 를 동적 주입.

## [1.8.0] - 2026-08-03

### Added
- **귀여운 커서**: 기본 커서를 통통한 파스텔 화살표(+반짝이)로, 버튼/탭/목록 등 클릭 가능한 곳은 고양이 발바닥으로 변경. 터미널 텍스트 영역·리사이즈 핸들·드래그 바는 선택/조작 UX 보존을 위해 원래 커서(text/resize/grab) 유지.

### Fixed
- **끊김 후 "재시도해도 먹통" 문제(좀비 웹소켓)**: 절전·잠금·네트워크 블립 후 WebSocket 이 죽었는데도 `close` 가 안 떠 입력이 먹통이 되고 자동 재연결도 안 걸리던 문제를 해결.
  - **하트비트(ping/pong)**: 10초마다 앱 레벨 ping → 서버 pong. 2회(~20초) 무응답이면 좀비로 판정해 강제 재연결. 서버도 15초마다 프로토콜 ping 으로 죽은 소켓을 정리(pty 는 재부착 대비 유지).
  - **깨어남 자동복구**: 창 복귀/`online`/`visibilitychange(보임)` 시 즉시 연결을 점검하고, 죽었으면 버튼 없이 자동 재연결. 다음날 로그인 후에도 스스로 이어붙음.
  - **강제 재시도**: "지금 재시도" 버튼과 자동 복구가 옛(좀비) 소켓을 확실히 폐기하고 새 소켓으로 즉시 연결 → 더 이상 강제 새로고침이 필요 없음.
  - 끊긴 상태에서 입력하면 즉시 재연결을 시도.

## [1.7.0] - 2026-07-28

### Changed
- **세션 영속 + 재부착(reattach)**: PTY(claude/셸)를 WebSocket 에 종속시키지 않고 서버에 세션으로 유지한다. 화면 잠금/절전/네트워크 블립 등으로 ws 가 끊겨도 **claude 는 계속 살아있고**, 재연결하면 **그 살아있는 세션에 그대로 다시 붙는다**(재시작이 아니라 이어서). 재부착 시 서버가 최근 출력 버퍼(최대 256KB)를 재생해 화면 상태를 복원.
  - 패널마다 안정적 `key` 부여 → 재연결이 같은 세션을 찾아 재부착.
  - 실제 종료는 **패널 X(`{type:'kill'}`)** 또는 프로세스 자체 종료 시에만. `⟳ 새로고침`은 새 key 로 재시작.
  - (한계: 렌더러를 완전히 새로 띄우면 in-memory key 가 사라져 기존 세션 재부착 불가 — 추후 key 영속화로 개선 예정. 서버 재시작은 여전히 모든 세션 종료.)

## [1.6.0] - 2026-07-28

### Added
- **세션별 작업 완료 표시**: 벨(완료) 발생 시 OS 알림 제목/본문에 세션 이름을 넣고(세션별 태그), 앱을 보고 있어도 어느 세션이 끝났는지 알 수 있도록 해당 패널에 초록 테두리 + 점 펄스 표시. 그 패널을 클릭/입력하면 표시 해제.
- **자동 재연결(백오프)**: 연결이 끊기면 1→2→4→8초 간격으로 자동 재연결 시도(최대 6회), 서버가 다시 뜨면 자동 복구. 배너에 "n초 후 재시도 (i/6)" 상태 표시 + "지금 재시도" 버튼. 서버 pty 는 ws 종료 시 함께 종료되므로 중복 세션은 생기지 않음.

### Fixed
- 재연결 버튼을 눌러도 반응 없던 문제: 상태 피드백 + 자동 재시도로 개선(서버가 꺼져 있으면 복구 후 자동 연결).

## [1.5.0] - 2026-07-28

### Added
- **터미널 글꼴 선택**: 헤더에 글꼴 드롭다운 추가(Cascadia Code / Cascadia Mono / JetBrains Mono / Fira Code / D2Coding / Consolas). 기본값을 Cascadia Code 스택으로 개선. 모든 열린 패널에 즉시 반영 + `localStorage` 저장.

### Changed
- **알림을 "작업 완료" 시에만 발생하도록 변경**: 기존에는 세션에 새 메시지가 감지될 때마다(매 메시지) 알림이 떠서 과도했음. 이제 **터미널 벨(`\x07`, Claude 가 턴 완료/입력 대기 시 울림)에서만** 알림(1.5s 디바운스). 벨을 울리지 않는 환경에서는 알림이 발생하지 않음.

## [1.4.0] - 2026-07-25

### Added
- **새 메시지 OS 알림**: 재개 세션에 새 메시지가 감지되면(창이 백그라운드일 때) Electron 네이티브 알림(Windows 토스트) + 작업표시줄 아이콘 깜빡임(`flashFrame`)으로 알려준다. 알림 클릭 시 앱 창 포커스. 브라우저 탭 모드에서는 웹 `Notification` API로 폴백. 창이 이미 포커스 상태면 기존 새로고침 버튼 펄스만 표시(중복 알림 방지).
- **터미널 글자 크기 조절**: 헤더에 `A−`/`A+` 버튼과 `Ctrl +`/`Ctrl -`/`Ctrl 0`(초기화) 단축키 추가. 모든 열린 터미널 패널에 즉시 반영되고 `localStorage`에 저장되어 다음 실행에도 유지.

## [1.3.0] - 2026-07-24

### Added
- **다른 계정(프로필) 세션 이어가기**: LNB(최근/검색)에 "모든 계정" 토글 추가 → 모든 프로필의 세션을 한 목록에서 확인.
  - 방식 A(원 계정으로 열기): 다른 계정 세션을 클릭하면 해당 계정 탭으로 전환 후 resume.
  - 방식 B(현재 계정으로 이어가기): 세션 항목의 "○○ 계정으로 이어가기" 버튼 → 대화 트랜스크립트를 현재 계정으로 **복사(원본 유지)** 후 resume. 사용량 한도로 계정 교체 시 활용.
  - 서버: `/api/recent`·`/api/search`에 `profile=__all__` 지원(각 세션에 소속 프로필 태그), `POST /api/session/copy`(원본 유지, 대상에 이미 있으면 덮어쓰지 않음).

## [1.2.0] - 2026-07-24

### Added
- **macOS/Linux 지원**: `claude-hub-app.command` 런처 추가(Finder 더블클릭 실행), Dock 아이콘 지정(`app.dock.setIcon`), `electron/icon.png`(ICO에서 추출한 256×256) 추가. 플랫폼별 아이콘 자동 선택(Windows `.ico` / 그 외 `.png`).
- **웹 미리보기 파일 지원 확대**: 로컬 파일 경로(`C:\...`, UNC `\\`, POSIX `/...`, `~/...`)와 `file:` URL을 서버 `/api/file`로 라우팅해 브라우저·앱 양쪽에서 렌더. `/api/file`이 이미지/오디오/비디오/PDF/텍스트·코드 등 폭넓은 형식을 인라인(`Content-Disposition: inline`)으로 서빙하고, 확장자 미매핑 파일은 앞부분을 읽어 텍스트/바이너리를 판별.

### Fixed
- 작업표시줄 고정 시 "Electron"이 아닌 앱으로 고정되도록 브랜딩된 exe(`Claude Terminal Hub.exe`) 실행 경로 정리 (Windows).
- 터미널 세션 패널 헤더의 아이콘-텍스트 줄바꿈 잔여 문제 (flex + `<span>` 래핑으로 정렬 고정).

### Removed
- 가독성이 나쁜 라이트 테마 2종 제거: `라이트 (기본)`, `Solarized Light` (저장돼 있던 경우 자동으로 다크로 폴백).

## [1.1.1] - 2026-07-24

### Added
- 앱 아이콘 추가 (`electron/icon.ico`, 터미널 프롬프트 `>_` 모양, 앱 색상 반영) — BrowserWindow·바탕화면 바로가기에 적용
- Electron 앱에 AppUserModelID 지정 (작업표시줄에 "Electron"이 아닌 앱 이름으로 표시)
- 바탕화면 바로가기(`Claude Terminal Hub.lnk`) 생성 — 작업표시줄 고정 가능
- 세션 목록/검색창 레이아웃 버그 수정(아이콘-텍스트 줄바꿈, 검색 아이콘 겹침), 폴더 선택 모달 버튼 위치 개선

## [1.1.0] - 2026-07-24

### Added
- 색상 팔레트 12종 (다크/라이트 포함, WCAG AA 대비 검증 완료) — 헤더 팔레트 아이콘에서 선택
- 강조(하이라이트) 색상 팔레트와 별개로 선택 가능
- `claude-hub-app` 실행 시 Electron 바이너리 미설치면 설치 여부 확인 후 진행
- `/health`에 버전 정보 노출

### Fixed
- xterm 기본 DOM 렌더러 → Canvas 렌더러 애드온으로 교체 (Claude Code처럼 화면을 자주 다시 그리는 TUI에서 렌더링이 깨지던 문제)
- 허브 프로세스가 `NO_COLOR` 환경(예: 에이전트 셸)에서 뜨면 자식 셸까지 색이 꺼지던 문제 — `cleanEnv()`에서 명시적으로 제거
- 라이트/다크 테마 전환 시 반영 안 되던 하드코딩 색상 5곳(`#tabs`, 버튼 hover, `.gutter`, 파일/세션 목록 구분선)
- `claude-hub-app.cmd`/`.vbs` 경로 하드코딩 — 다른 PC에 클론하면 실행 안 되던 문제

### Docs
- 프로세스 무차별 종료 금지 규칙 추가 (PID 특정 없는 `taskkill /IM` 등 금지)

## [1.0.0] - 이전

최초 버전 — 세션 관리(split 뷰, 프로필별 계정 분리), 아티팩트/파일 미리보기, 세션 이름 자동 반영 등.
